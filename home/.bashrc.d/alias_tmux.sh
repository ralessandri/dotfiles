alias tmn='tmux new-session -s "$(basename "$PWD")"'

alias tml='tmux list-sessions'

alias tma='tmux a'
alias tmua='tmux attach-session -t'

alias tmks='tmux kill-session -t'
alias tmksa='tmux kill-session -a'