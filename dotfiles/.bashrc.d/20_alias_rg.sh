# List files with ripgrep matches
alias rgf='rg --files-with-matches'

# List hidden files with ripgrep matches
alias rgfh='rg --files-with-matches --hidden'

# Search hidden files with ripgrep
alias rgh='rg --hidden'

# Search hidden and ignored files with ripgrep
alias rga='rg --hidden --no-ignore'
