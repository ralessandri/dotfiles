# Search case-insensitively with ripgrep
alias rgi='rg -i'

# Search with smart case sensitivity using ripgrep
alias rgs='rg -S'

# List files with ripgrep matches
alias rgf='rg -l'

# List hidden files with ripgrep matches
alias rgfh='rg -l.'

# Count ripgrep matches per file
alias rgc='rg -c'

# Search hidden files with ripgrep
alias rgh='rg -.'

# Show three lines after each ripgrep match
alias rga='rg -A 3'

# Show three lines before each ripgrep match
alias rgb='rg -B 3'

# Show three lines around each ripgrep match
alias rgx='rg -C 3'
